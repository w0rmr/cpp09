#pragma once 

#include <string>
#include <iostream>
#include <exception>
#include <vector>
#include <algorithm>

class RPN {
    private:
        std::vector<int> elm;
    public:
        RPN(std::string args);
        // ~RPN();

};